= semantic_form_for @credit_amount, :url => save_amount_tenants_path do |f|
  = f.inputs do
    = f.hidden_field :tenant_id, :value => @tenant_id
    -#= f.input :amount
    = f.input :amount, :as => :number, :input_html => { :min => 0, max: 9999 }
  = f.actions
  = link_to 'Cancel', tenants_path
